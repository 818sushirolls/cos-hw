apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webmvc.fullname" . }}-test-connection"
  namespace: {{ .Values.namespace | default "default" }}
  labels:
    {{- include "webmvc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webmvc.fullname" . }}:{{ .Values.service.port }}/hc']
  restartPolicy: Never
